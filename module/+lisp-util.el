;;; +lisp-util.el --- Summery
;;; -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package dash
    :config (dash-enable-font-lock))
(use-package f)
(use-package s)

(provide '+lisp-util)
;;; +lisp-util.el ends here
