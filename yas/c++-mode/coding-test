# -*- mode: snippet -*-
# name: codeing test template
# key: code-test
# --

#include <iostream>

using namespace std;

int main(int argc, char* argv[]) {
    ios::sync_with_stdio(false);
    cin.tie(0);

    

    return 0; 
}
